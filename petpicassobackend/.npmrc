# Ensure npm uses the correct registry
registry=https://registry.npmjs.org/

# Disable audit during install to speed up build
audit=false

# Use legacy peer deps if needed
legacy-peer-deps=true

# Ensure package-lock.json is used
package-lock=true
